# Dépendances
/node_modules
/.pnp
.pnp.*
.yarn/*

# Build / cache Next.js
/.next/
/out/
next-env.d.ts

# Variables d'environnement (NE JAMAIS PUSH — contient DATABASE_URL)
.env
.env*
!.env.example

# Base de données locale
*.sqlite
*.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Divers système / éditeur
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# Turbopack / SWC
/.turbo
.swc

# Cache TypeScript
*.tsbuildinfo

# Paquets Git
*.bundle

# Archives locales (ne jamais pousser)
*.zip
*.tar.gz

# Contexte local du CLI Neon ( IDs projet/branche, ne contient pas de secret mais propre à la machine)
.neon/
neon.json

# Contexte local du CLI Neon + dumps d'import (usage ponctuel, jamais poussés)
.neon/
neon.json
import-neon*.sql
